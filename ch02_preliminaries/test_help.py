import torch
from torch.distributions import multinomial
import my_plt

print("dir(torch.distributions) : ", dir(torch.distributions))
print("----------------------------")
print("help(torch.ones) : ", help(torch.ones))
